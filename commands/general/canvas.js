const Canvas = require('canvas'); 

module.exports.run = async (bot, message, arg) => {

    const canvar = Canvas-Canvas.createCanvas(700,250);

    const ctx = canvas-getContext('2d');
}

module.exports.config = { 
    name: "canvas",
    aliases: []
}